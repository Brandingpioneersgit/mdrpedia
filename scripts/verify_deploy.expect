#!/usr/bin/expect -f

set timeout 20
set ip "72.61.224.90"
set user "root"
set password "BPtools@54321"

spawn ssh -o StrictHostKeyChecking=no $user@$ip

expect {
    "password:" {
        send "$password\r"
    }
}

expect "#"
send "pm2 list\r"
expect "#"
send "netstat -tulpn | grep node\r"
expect "#"
send "curl -I http://localhost:4321\r"
expect "#"
send "curl -I http://localhost:3000\r"
expect "#"
send "exit\r"
expect eof
