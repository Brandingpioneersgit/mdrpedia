#!/usr/bin/expect -f

set timeout 20
set ip "72.61.224.90"
set user "root"
set password "BPtools@54321"
set local_file ".env"
set remote_path "/root/mdrpedia/.env"

spawn scp -o StrictHostKeyChecking=no $local_file $user@$ip:$remote_path

expect {
    "password:" {
        send "$password\r"
    }
}

expect eof
